## Temporary Files

* All generated files belong in the `tmp/` directory.
* The directory is ignored by Git – only `.gitkeep` / `README.md` are tracked.
* Before pushing, run `./scripts/clean-tmp.sh` or ensure no temporary files remain.
